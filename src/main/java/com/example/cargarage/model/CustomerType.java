package com.example.cargarage.model;

// Enum to represent types of customers in the system.
// REGULAR - A first-time or non-frequent customer
// REPEAT  - A customer who has previously availed services (loyal/returning)
public enum CustomerType {
    
    REGULAR,
    REPEAT
}
